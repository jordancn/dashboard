query TransactionVendor($transactionId: ID!) {
  transaction(transactionId: $transactionId) {
    id
    vendor {
      id
      name
      image
    }
  }
  vendors {
    id
    name
    image
  }
}
