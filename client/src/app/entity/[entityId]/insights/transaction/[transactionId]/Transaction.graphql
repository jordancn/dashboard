query Transaction($transactionId: ID!) {
  transaction(transactionId: $transactionId) {
    id
    account {
      id
      name
      number
    }
    date
    category {
      id
      name
    }
    description
    amount
    pending
    vendor {
      id
      name
      image
    }
  }
}
